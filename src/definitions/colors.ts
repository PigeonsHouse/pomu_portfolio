export const withAlpha = (color: string, alpha: number) =>
  `rgb(from ${color} r g b  / ${alpha})`;

export const Color = {
  White: "#FFFFFF",
  Base: "#665048",
  Background: "#F2CEDD",
  Gray: "#878787",
};
